---
name: Position
---

import { PropsTable } from 'docz';
import Position from './Position';
import BasicExample from './examples/basic.example';
import MountExample from './examples/mount.example';

import {
  LiveProvider,
  LiveEditor,
  LiveError,
  LivePreview
} from 'react-live';

# Position

Position is a low-level component that reports the cursor position on each `mousemove` event on the `onChange` callback function. It's used in `Surface` components.

## Properties

<PropsTable of={Position}/>

## Examples

The example below continously reads the user's mouse coordinates:

<LiveProvider code={BasicExample} scope={{Position}}>
  <LiveEditor />
  <LiveError />
  <LivePreview />
</LiveProvider>

We can conditionally add the Position component to the DOM when the user holds down the mouse (on the `mousedown` event), and stop reading the coordinates on `mouseup`:

<LiveProvider code={MountExample} scope={{Position}}>
  <LiveEditor />
  <LiveError />
  <LivePreview />
</LiveProvider>

> 💡 These examples use less than ideal React idioms to make them shorter. For better performance, avoid passing inline functions as callbacks.

